{
  "routes": [
    {
      "method": "GET",
      "path": "/works",
      "handler": "works.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/works",
      "handler": "works.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/works",
      "handler": "works.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
